#!/bin/bash

# Todo: This is a fucking dummy way to "deploy"

dTime=$(date +%Y.%m.%d-%H:%M:%S)

rm -r dist/*

if [ -f "client/app/config.js" ]; then
  echo 'Config exist. Moving forward'
else
  echo 'Config not found'
  cp client/app/config.dist.js client/app/config.js
fi

yarn run build
#cp .htaccess dist
#git add dist/*
#git commit -m "Deploy to prod ${dTime}"
#git push
